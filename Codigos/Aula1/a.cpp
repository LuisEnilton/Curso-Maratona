#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    // ler a entrada
    int n; cin >> n;
    // processar
    int resposta = 2 * n;
    // imprimir a saida
    cout << resposta << endl;
    return 0;
}