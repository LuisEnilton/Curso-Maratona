#include <bits/stdc++.h>
#define endl '\n'
using namespace std;
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int x; cin >> x;
    cin.ignore();
    string s;
    getline(cin, s);

    cout << s << endl;
    return 0;
}