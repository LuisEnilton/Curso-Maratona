#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int n; cin >> n;

    cout << (n <=36 ? "Bem vindo ao Primeiro Periodo!": "Bem vindo ao Segundo Periodo!") << endl;
    return 0;
}